//! Placeholder. The real server arrives in milestone M4 — until then this
//! crate exists so the workspace shape is settled from day one.

fn main() {
    println!("catan-server: nothing here yet — the engine comes first (see M1-M3).");
}
